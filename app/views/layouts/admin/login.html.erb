<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
  <meta name="description" content="">
  <meta name="author" content="">


  <title>TOP SECRET!</title>

  <%= stylesheet_link_tag 'admin/login', media: 'all', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>


  <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
  <!--[if lt IE 9]>
  <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
  <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
  <![endif]-->
</head>

<body>

<div class="container" style="vertical-align: middle">

  <%= form_tag admin_sessions_path, :class => 'form-signin' do %>
      <h2 class="form-signin-heading">TOP SECRET</h2>
      <% if flash[:notice] %>
        <div class="alert alert-success">
          <%= flash[:notice] %>
        </div>
      <% end %>

      <% if flash[:alert] %>
          <div class="alert alert-danger">
            <%= flash[:alert] %>
          </div>
      <% end %>

      <%= text_field_tag :email, params[:email], class: 'form-control', placeholder: 'Email Address' %>
      <%= password_field_tag :password, '', class: 'form-control', placeholder: 'Password' %>
      <%= submit_tag 'See the Secrets', class: 'btn btn-primary btn-block' %>
      <%= link_to 'Back to Safe Waters', root_path, class: 'btn btn-default btn-block' %>
  <% end %>

</div> <!-- /container -->

</body>
</html>
